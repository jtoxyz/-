name: Obsolete Excel rebuild workflow

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This one-time workflow is obsolete and will be removed by the security hardening workflow."
